import static org.junit.Assert.assertEquals;

import java.util.*;

import javax.xml.transform.TransformerException;

import org.junit.*;

import Converter.*;
import Converter.Currency;

public class ParserTest {
	/*private GetXML getXML;
	private Parser parser;
	private Currency cur;
	private Map<String, Currency> curMap;
	
	@Before
	public void setUp() {
		getXML = new GetXML();
		curMap = new HashMap<String,Currency>();
		try {
			parser = new Parser(getXML.getDoc());
		} catch (TransformerException e) {
			e.printStackTrace();
		}
		cur = new Currency("euro","EUR", 4.2777);
		curMap.put("euro", cur);
		
	}
	@Test
	public void shouldSayThatIsTheSame() {
		parser.setMap("euro", cur);
		System.out.println(parser.getParserMap());
		assertEquals("xxx", curMap, parser.getParserMap());
		
	}*/
	
}
